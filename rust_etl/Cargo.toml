[package]

name = "rust_etl"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { vesion = "1", features = ["full"]}
reqwest = { vesion = "1.0", features = ["json", 'gzip'] }
serde = { version = "1.0", features = ["derive""] }

